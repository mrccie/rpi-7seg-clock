# save all cronjobs by root user into listofcronjobs.txt text file (serves as backup too)
crontab -l > listofcronjobs.txt
# then edit listofcronjobs.txt
# then reload edited listofcronjobs.txt
crontab listofcronjobs.txt


!!!!!!!! To Remove a job:
crontab -e

^ from sudo, if done from root prompt




!!!!!!!!!!!!... stuff I've done prior to that ...!!!!!!!!!!!!!!!!!!

# Create a cron file for the user 'pi'
#   No sudo for 2nd part?
sudo touch /var/spool/cron/pi
/usr/bin/crontab /var/spool/cron/pi


# Create a bogus test job - Attempt 1  <--- Fail
#  Permission denied!
sudo echo "* * * * * rm -f /home/pi/rpi_7seg/testdir/*" >> /var/spool/cron/pi

try instead:
sudo echo "* * * * * echo Hello >> /home/pi/rpi_7seg/testdir/hello.txt" >> /var/spool/cron/pi


# Create a bogus test job - Attempt 2  <--- Still fail
# Create the file /etc/cron.allow
#  Add the user pi to it
cat /etc/cron.allow
pi

# Create a bogus test job - Attempt 3  <--- Still fail(?!)
# Add root to the file...
cat /etc/cron.allow
root
pi

